#!/bin/sh

set -u

if command -v npx.cmd >/dev/null 2>&1; then
  NPX_BIN="npx.cmd"
else
  NPX_BIN="npx"
fi

echo "Deploying brazilian-massage to Vercel production before push..."
"$NPX_BIN" vercel --prod --yes --scope clarissamaria-stars-projects
