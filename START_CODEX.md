Codex — start here

1) Clone the repo:
   git clone https://github.com/contatofoxads-gif/brazilian-massage.git
   cd brazilian-massage

2) Branching (optional):
   git checkout -b codex-build

3) Follow the instructions in PLANNING.md exactly.
   Priority order: vercel.json → css/style.css → js/quiz.js → q1..q5.html → result.html → invite.html → assets/

4) Commits and PRs:
   - Make small, incremental commits per file/group.
   - Push branch and open a PR when the full skeleton is ready.

5) When finished, deploy to Vercel and post the preview URL in the PR.

Notes:
- Do NOT use the word "tantra" in domain names or image filenames.
- Use neutral wellness imagery only (no nudity, no sexual content).
- All copy in English, tone: male, 45+, international.

If you need clarifications, open an issue named "QUESTION: implementation detail".
